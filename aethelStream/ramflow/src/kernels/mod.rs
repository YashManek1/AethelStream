// src/kernels/mod.rs — thin Rust wrappers around compiled CUDA kernels
//
// In Sprint 0 this module is empty.  Later sprints will expose safe wrappers
// like `kernels::launch_overflow_check(stream, buf, len)`.
